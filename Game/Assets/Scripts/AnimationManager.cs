using UnityEngine;

public class AnimationManager : MonoBehaviour
{

    /*private void Start()
    {
        
        if (Gashahn = true, Arbeitsplatz = beliebig, Schlüsselloch = alles\Schlüssel, In der Luft = nix, AufDerTür = nix) {
            // keine änderung (hinterrücks erschlagen mit gasexplosion)
        }

        if (Gashahn = false, Arbeitsplatz = beliebig, Schlüsselloch = beliebig\Schlüssel, In der Luft = nix, AufDerTür = nix) {
            // keine änderung (hinterrücks erschlagen ohne explosion)
        }

        if (Gashahn = beliebig, Arbeitsplatz = beliebig, Schlüsselloch = beliebig\Schlüssel, In der Luft = beliebig, AufDerTür = beliebig/kaktus) {
            // person kriegt dings ins gesicht, haut ab (person wird dabei gewarnt)
        }

        if (Gashahn = beliebig, Arbeitsplatz = beliebig, Schlüsselloch = beliebig\Schlüssel, In der Luft = beliebig, AufDerTür = kaktus) {
            // person kriegt kaktus ins gesicht, wird überwältigt
        }

        if (Gashahn = beliebig, Arbeitsplatz = beliebig\Pistole, Schlüsselloch = beliebig\Schlüssel, In der Luft = beliebig, AufDerTür = nix) {
            // person wird gewarnt, personen erstechen sich gegeseitig
        }

        if (Gashahn = beliebig, Arbeitsplatz = Pistole, Schlüsselloch = beliebig\Schlüssel, In der Luft = beliebig, AufDerTür = nix) {
            // person wird gewarnt, erschießt mörder
        }

        if (Gashahn = beliebig, Arbeitsplatz = beliebig, Schlüsselloch = Schlüssel, In der Luft = beliebig, AufDerTür = beliebig) {
            // person kommt nicht rein (tür wird bei schlüsselanimation zugemacht) (Fluransicht)
        }

        
    }*/
    
}
